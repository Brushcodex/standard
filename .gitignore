# Node / package tooling
node_modules/
dist/
build/
*.tsbuildinfo
.turbo/

# Generated corpus copy for @brushcodex/fixtures (single source of truth is examples/);
# regenerated by the package's build/prepack/pretest, shipped in the tarball via "files".
packages/fixtures/corpus/

# Locally packed tarballs (Phase 4 artifacts) — never committed
artifacts/

# Logs / caches
*.log
.cache/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Never commit secrets or private data into the public-candidate standard repo
.env
.env*.local
