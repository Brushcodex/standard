@AGENTS.md

<!--
  Single source of truth: AGENTS.md. This file is one import line on purpose.

  Claude Code resolves `@AGENTS.md` and inlines that file, so Claude and Codex
  read the same briefing and cannot be told different things. Do NOT add project
  guidance here — Codex reads AGENTS.md only, so anything written here would be
  invisible to it. Put every durable convention in AGENTS.md.
-->
